#include "main.h"

int get_func(const char *format, va_list ap)
{/*
	 print_f prn[] = {
		{'c', print_c}, 
        {'%', print_p},
        {0, NULL}
    };
    int i = 0;
    int a = 0;

	while (i < 3)
	{
		if (prn[i].p == s)
			a += prn[i].f(ap);
		i++;
	}
	write(1, &s, 1);
	return (a);
    */
}